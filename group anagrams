class Solution:
    def groupAnagrams(self, strs):
        m = {}
        for s in strs:
            k = ''.join(sorted(s))
            if k not in m:
                m[k] = [s]
            else:
                m[k] += [s]
        return list(m.values())
